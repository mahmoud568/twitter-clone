import { AtHashColorDirectiveDirective } from './at-hash-color-directive.directive';

describe('AtHashColorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AtHashColorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
